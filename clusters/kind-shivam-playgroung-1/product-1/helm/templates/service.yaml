apiVersion: v1
kind: Service
metadata:
  name: {{ include "product-1.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "product-1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "product-1.selectorLabels" . | nindent 4 }}
