# Shared with setup-argocd/helmfile.yaml (bootstrap) and Argo CD Application (GitOps).
# Single source of truth for Argo CD Helm values on this cluster.

server:
  service:
    type: ClusterIP
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 256Mi

configs:
  params:
    server.insecure: false

  cm:
    # Must match how you open the UI (see RUNBOOK OIDC notes).
    url: https://localhost:8080
    oidc.config: |
      name: Azure
      issuer: https://login.microsoftonline.com/939f575e-0369-4929-a7b3-9c18af593c11/v2.0
      clientID: bdf1cc2b-2d99-42a4-92ce-7b950257c64f
      clientSecret: $argocd-oidc-azure:clientSecret
      requestedScopes:
        - openid
        - profile
        - email

  rbac:
    policy.default: role:viewer
    scopes: "[groups, email]"
    policy.csv: |
      p, role:platform-admin, applications,    *, */*, allow
      p, role:platform-admin, applicationsets, *, */*, allow
      p, role:platform-admin, clusters,        *, *,   allow
      p, role:platform-admin, repositories,    *, *,   allow
      p, role:platform-admin, accounts,        *, *,   allow
      p, role:platform-admin, logs,            *, */*, allow
      p, role:platform-admin, exec,            *, */*, allow

      p, role:product-1-engineer, applications, get,      product-1/*, allow
      p, role:product-1-engineer, applications, sync,     product-1/*, allow
      p, role:product-1-engineer, applications, override, product-1/*, allow
      p, role:product-1-engineer, applications, action,   product-1/*, allow
      p, role:product-1-engineer, logs,         get,      product-1/*, allow

      p, role:product-2-engineer, applications, get,      product-2/*, allow
      p, role:product-2-engineer, applications, sync,     product-2/*, allow
      p, role:product-2-engineer, applications, override, product-2/*, allow
      p, role:product-2-engineer, applications, action,   product-2/*, allow
      p, role:product-2-engineer, logs,         get,      product-2/*, allow

      p, role:viewer, applications,  get, */*, allow
      p, role:viewer, repositories,  get, *,   allow

      g, platform-engineers, role:platform-admin
      g, team-a,             role:product-1-engineer
      g, team-b,             role:product-2-engineer
      g, admin, role:platform-admin

controller:
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi

repoServer:
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 1Gi

applicationSet:
  resources:
    requests:
      cpu: 25m
      memory: 32Mi
    limits:
      cpu: 100m
      memory: 128Mi

notifications:
  resources:
    requests:
      cpu: 25m
      memory: 32Mi
    limits:
      cpu: 100m
      memory: 128Mi

dex:
  resources:
    requests:
      cpu: 10m
      memory: 32Mi
    limits:
      cpu: 50m
      memory: 64Mi

redis:
  resources:
    requests:
      cpu: 25m
      memory: 32Mi
    limits:
      cpu: 100m
      memory: 128Mi
