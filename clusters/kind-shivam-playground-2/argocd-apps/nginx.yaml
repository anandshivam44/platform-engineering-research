apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kind-shivam-playground-2.nginx
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/anandshivam44/platform-engineering-research.git
      targetRevision: main
      ref: values
    - repoURL: https://charts.bitnami.com/bitnami
      chart: nginx
      targetRevision: "22.5.3"
      helm:
        valueFiles:
          - $values/k8s-manifests/nginx/values.yaml
          - $values/k8s-manifests/nginx/helm-overrides/kind-shivam-playground-2.yaml
  destination:
    server: https://172.18.0.3:6443
    namespace: nginx
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
